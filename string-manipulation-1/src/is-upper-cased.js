/* exported isUpperCased */
/* -check to see o if all letters are capitalized,
- if not, return as false */
function isUpperCased(word) {

  if (word !== word.toUpperCase()) {
    return false;
  } else { return true; }

}
