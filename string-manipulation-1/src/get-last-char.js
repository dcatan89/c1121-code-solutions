/* exported getLastChar */

/* -Find the last character of the unknown word
- Identify what character is at the end of the word,
-Return the output of that value */

function getLastChar(string) {
  return string[(string.length - 1)];
}
